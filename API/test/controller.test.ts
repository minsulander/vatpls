describe("Database", () => {
  it("Database connection OK", () => {
    expect(false).toBe(false);
  });
  it("Table controller exist", () => {
    expect(false).toBe(true);
  });
   it("", () => {
    expect(false).toBe(true);
  });
    it("Table controller exist", () => {
    expect(false).toBe(true);
  });
  
  it("Table controller exist", () => {
    expect(false).toBe(true);
  });
   it("Table controller exist", () => {
    expect(false).toBe(true);
  });
    it("Table controller exist", () => {
    expect(false).toBe(true);
  });
    it("Table controller exist", () => {
    expect(false).toBe(true);
  });
  


 

 

})


describe("Add new controller", () => {
  it("saves controller in database", () => {
    expect(true).toBe(true);
  })
})